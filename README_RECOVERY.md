# AutoBBoxToolkit Workspace (Recovered)

This folder was rebuilt from remaining runtime artifacts after cleanup.

## Current status
- Runtime package is available in: `deploy/AutoBBoxToolkit`
- Runtime mirror is available in: `runtime/AutoBBoxToolkit`
- Ribbon/message/resources fallbacks are prepared under root `text/` and `ribbon/`

## Important limitation
Original C/C++ source files were not found in recycle bin or local snapshots.
This rebuild restores a complete working workspace structure, but not lost source code.

## Next recommended step
If historical source backup exists elsewhere, place it under `src/` and `include/`.
